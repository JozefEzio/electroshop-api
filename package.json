{
  "name": "your-json-server-project",
  "version": "1.0.0",
  "description": "JSON Server deployed to Koyeb",
  "main": "server.js",
  "scripts": {
    "start": "node server.js", 
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^5.2.1",
    "json-server": "^1.0.0-beta.3"
  }
}